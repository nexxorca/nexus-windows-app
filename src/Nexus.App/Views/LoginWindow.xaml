<Window x:Class="Nexus.App.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Nexus Desktop — Login"
        Width="400" Height="320"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Nexus Desktop" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>

        <TextBlock Grid.Row="1" Text="Nexus URL" Margin="0,0,0,3"/>
        <TextBox Grid.Row="2" x:Name="txtUrl" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="3" Text="Email" Margin="0,0,0,3"/>
        <TextBox Grid.Row="4" x:Name="txtEmail" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="5" Margin="0,0,0,3">
            <TextBlock Text="Password"/>
        </StackPanel>

        <StackPanel Grid.Row="6">
            <PasswordBox x:Name="txtPassword" Margin="0,0,0,15"/>
            <TextBlock x:Name="lblError" Foreground="Red" TextWrapping="Wrap" Margin="0,0,0,10" Visibility="Collapsed"/>
            <Button x:Name="btnLogin" Content="Login" Height="32" Click="BtnLogin_Click"/>
        </StackPanel>
    </Grid>
</Window>
